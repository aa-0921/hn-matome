# 対応リスト（2026-03-27）

このファイルは、現在の運用状況と今後の対応を1つにまとめた実行用リストです。

---

## まず必須（未対応だと運用が止まる）

- [x] `OPENROUTER_API_KEY` を GitHub Secrets に登録済み
- [x] `CORE_REPO_TOKEN` を GitHub Secrets に登録済み
- [ ] Cloudflare Pages の本番設定を最終確認
  - 確認手順:
    1. Cloudflare Dashboard → Workers & Pages を開く
    2. `hn-matome`（任意名）プロジェクトの有無を確認
    3. プロジェクトが **ない場合** は以下で新規作成
       - `Create application` → `Pages` → `Connect to Git`
       - GitHub リポジトリ `aa-0921/hn-matome` を選択
       - Production branch: `main`
       - Build command: 空欄
       - Build output directory: `docs`
       - `Save and Deploy`
    4. 既存/新規どちらでも `Settings` → `Builds & deployments` を開く
    5. 以下を確認
       - `Production branch`: `main`
       - `Build command`: 空欄
       - `Build output directory`: `docs`
    6. `Deployments` タブで最新デプロイが `Success` であることを確認
    7. 公開URLで以下を確認
       - `/` が表示される
       - `/archive/YYYY-MM-DD.html` が表示される
       - `/sitemap.xml` が表示される
- [x] Actions `workflow_dispatch` の手動実行確認
  - 対象ワークフローは **`HN日報 毎日更新`** で正しい
  - ログに出る以下の処理は想定どおり
    - `rm -rf docs/...`
    - `cp -R core/docs/. docs/`
  - 意味:
    - 旧成果物を消して、`core` 側で新規生成した `docs` を毎回丸ごと同期している
    - 失敗ではなく、再現性を保つための通常処理
  - 合格条件:
    - ジョブ全体が `Success`
    - 実行後コミットに `docs/archive/*.html`、`docs/pagefind/*`、`docs/sitemap.xml` の更新が含まれる
  - 実施ログ（確認済み）:
    - Run ID: `23586757224`
    - Event: `workflow_dispatch`
    - Conclusion: `success`
    - Duration: 約 14分31秒
    - URL: [GitHub Actions Run](https://github.com/aa-0921/hn-matome/actions/runs/23586757224)
    - `生成した docs をワークフロー対象リポジトリへ同期` ステップ成功を確認済み

---

## 早めにやるべき（ドキュメントと実態の不一致）

- [x] `MEMO.md` を現行構成（Cloudflare + public/private分離）に更新
- [x] `サービス設計書.md` のMVPチェックボックスを実装状況に合わせて更新
- [x] `docs/plans/2026-03-26-hn-matome-mvp.md` の完了チェックを更新
- [x] `memory/MEMORY.md` と `docs/plans/public-private-repo-split.md` を public/private 分離方針で統一

---

## 品質面でやったほうが良い対応（優先度: 中）

- [x] `HNClient.fetch_top_stories_for_date` のテスト追加
- [x] `fetch_and_generate.py` の障害時挙動を強化（記事単位で例外を握り、全体停止を回避）
- [x] LLMの最小監視ログを追加（翻訳・要約の成功/失敗件数を出力）
- [x] `privacy.html` の最終更新日を固定値から自動埋め込みに変更

---

## 収益化・集客で次に効く対応（優先度: 中〜低）

- [ ] Google Search Console 登録 + `sitemap.xml` 送信
  - 手順:
    1. [Google Search Console](https://search.google.com/search-console) にログイン
    2. 「プロパティを追加」→ ドメインまたはURLプレフィックスを登録
    3. 所有権確認（DNS TXT か HTML ファイル）
    4. 左メニュー `サイトマップ` → `https://<your-domain>/sitemap.xml` を送信
    5. `ページ` レポートで `archive/` がインデックスされるか確認
- [ ] OGP画像自動生成
  - 最小実装案:
    1. `scripts/ogp_generator.py` を追加（Pillowで1200x630画像生成）
    2. 日付・サービス名・上位見出しを描画
    3. `docs/assets/ogp/YYYY-MM-DD.png` に出力
    4. `archive.html` の `og:image` をその日付画像に差し替え
    5. `fetch_and_generate.py` から生成処理を呼ぶ
- [ ] 30日蓄積後に AdSense 申請
  - 手順:
    1. 独自ドメインを接続
    2. `about.html` / `privacy.html` の内容を最終確認
    3. 30日以上のアーカイブ蓄積を確認
    4. Search Console 側で重大なインデックスエラーがないことを確認
    5. AdSense でサイト審査申請
- [x] README は作成しない方針を維持
  - 代わりに `MEMO.md` を最新化して情報を集約

---

## 次の実行順（おすすめ）

1. Cloudflare Pages の本番設定確認
2. `HN日報 毎日更新` を手動実行して成功ログ確認
3. Search Console 登録と sitemap 送信
4. OGP画像自動生成の実装
5. 30日蓄積後に AdSense 申請

---

## 実施記録（2026-03-27追記）

### GitHub Actions

- 最新3件（`HN日報 毎日更新`）確認結果
  - `23623028144` / `schedule` / `success`
  - `23586757224` / `workflow_dispatch` / `success`
  - `23584765925` / `workflow_dispatch` / `failure`（過去失敗）
- 直近では `schedule` と `workflow_dispatch` の両方で成功実績あり

### Cloudflare Pages（手動確認記録テンプレート）

- 確認日: `____-__-__`
- Production branch: `main` / 確認結果: `____`
- Build command: 空欄 / 確認結果: `____`
- Output directory: `docs` / 確認結果: `____`
- 最新デプロイ状態: `____`
- 公開URL確認:
  - `/` : `OK / NG`
  - `/archive/YYYY-MM-DD.html` : `OK / NG`
  - `/sitemap.xml` : `OK / NG`

### 公開URLの実測結果（自動確認）

- 実行日時: 2026-03-27
- 確認URL:
  - `https://hn-matome.pages.dev/` → **想定外の別コンテンツが表示**
    - 表示例: 「HN 日々のまとめ - 2026年3月26日木曜日」
  - `https://hn-matome.pages.dev/archive/2026-03-27.html` → **404**
  - `https://hn-matome.pages.dev/sitemap.xml` → **404**
- 判定:
  - 現在の `hn-matome` リポジトリ成果物（`docs/`）が当該URLに配信されていない可能性が高い
  - Cloudflare Pages 側で「別プロジェクト / 別ブランチ / 別出力ディレクトリ / 別リポジトリ」を参照している可能性あり

### Cloudflare 側の確認ポイント（優先）

1. Pages プロジェクトに接続されている GitHub リポジトリ名が `aa-0921/hn-matome` か
2. Production branch が `main` か
3. Build output directory が `docs` か
4. 最新デプロイの Source Commit が Actions 後に更新された `main` の先頭コミットか
5. 旧プロジェクト（同じドメインを使っていたもの）が残っていないか
